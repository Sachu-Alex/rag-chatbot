# RAG ChatBot Configuration
# Copy this file to .env and customize as needed

# OpenAI Configuration (optional - only if you want to use GPT models)
# Get your API key from: https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-your-openai-api-key-here

# System Configuration
DEBUG=false
LOG_LEVEL=INFO

# Model Configuration
# Options: "huggingface" (free, local) or "openai" (requires API key above)
LLM_PROVIDER=huggingface

# Hugging Face Model (used when LLM_PROVIDER=huggingface)
# Good free options:
# - google/flan-t5-base (default, good for Q&A)
# - microsoft/DialoGPT-medium (conversational)
# - distilbert-base-uncased-distilled-squad (lightweight Q&A)
HF_MODEL_NAME=google/flan-t5-base

# OpenAI Model (used when LLM_PROVIDER=openai)
OPENAI_MODEL_NAME=gpt-3.5-turbo

# Processing Configuration
CHUNK_SIZE=1000
CHUNK_OVERLAP=200
EMBEDDING_MODEL=all-MiniLM-L6-v2

# Database Configuration
CHROMA_PERSIST_DIRECTORY=./chroma_db
COLLECTION_NAME=document_qa